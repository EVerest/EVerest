# The EVerest type headers themselves are generated by ev_add_project() (see
# cmake/everest-generate.cmake); this directory only hosts tests for the
# generated type serialization.
if(EVEREST_CORE_BUILD_TESTING)
    add_subdirectory(tests)
endif()
