[package]
name = "RsIskraMeter"
version = "0.1.0"
edition = "2021"
# Minimum toolchain, in sync with RUST_VERSION in MODULE.bazel.
rust-version = "1.86.0"

[build-dependencies]
everestrs-build = { workspace=true }

[dependencies]
anyhow = "1.0.75"
backon = "1.2.0"
chrono = "0.4.31"
everestrs = { workspace=true }
log = "0.4.20"
rand = "0.8.5"
mockall = { version = "0.12.1", optional = true }
mockall_double = { version = "0.3.1", optional = true}

[features]
default = ["mockall", "mockall_double"]
