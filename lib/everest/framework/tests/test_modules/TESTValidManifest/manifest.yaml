description: "This is a valid manifest."
config:
  valid_config_entry:
    description: "This is a valid config entry"
    type: string
    mutability: ReadWrite
  valid_config_entry_with_default:
    description: "This is a valid config entry with a default"
    type: number
    default: 42
  valid_bool_config_entry:
    description: "A boolean type config entry"
    type: boolean
    mutability: ReadOnly
    default: true
  module_id_default:
    description: "A string default containing the module instance ID"
    type: string
    default: "/logs/${module_id}"
    pattern: '^/logs/[a-zA-Z_][a-zA-Z0-9_.-]*$'
provides:
  main:
    description: "This unit has a valid manifest but no corresponding class"
    interface: "test_interface"
    config:
      valid_config_entry:
        description: "This is a valid config entry"
        type: string
metadata:
  license: "https://opensource.org/licenses/Apache-2.0"
  authors: ["Kai-Uwe Hermann"]
