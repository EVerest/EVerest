set(THIRDPARTY_INCLUDE_TMP_DIR ${LIB_INCLUDE_TMP_DIR}/3rd_party)
set(THIRDPARTY_INCLUDE_INSTALL_DIR ${LIB_INCLUDE_INSTALL_DIR}/3rd_party)

if(NOT EVEREST_LIBS_ONLY)
    add_subdirectory(nanopb)
endif()
